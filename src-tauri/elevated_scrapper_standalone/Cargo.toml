[package]
name = "elevated_scrapper"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bluetooth_model = { path = "../bluetooth_model" }
